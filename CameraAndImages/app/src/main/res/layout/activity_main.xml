<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnTakeSelfie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:onClick="butFirstLetMeTakeASelfie"
        android:text="Button"
        app:layout_constraintEnd_toEndOf="@+id/imgSelfie"
        app:layout_constraintStart_toStartOf="@+id/imgSelfie"
        app:layout_constraintTop_toBottomOf="@+id/imgSelfie" />

    <ImageView
        android:id="@+id/imgSelfie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/avatars" />

    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:onClick="butFirstLetMeTakeASelfie"
        android:text="Button"
        app:layout_constraintEnd_toEndOf="@+id/btnTakeSelfie"
        app:layout_constraintStart_toStartOf="@+id/btnTakeSelfie"
        app:layout_constraintTop_toBottomOf="@+id/btnTakeSelfie" />

    <Button
        android:id="@+id/btnGallery"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:onClick="next"
        android:text="Button"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/btnSave"
        app:layout_constraintStart_toStartOf="@+id/btnSave" />
</androidx.constraintlayout.widget.ConstraintLayout>